spring.application.name=order-service

# Database connection - values come from environment variables
spring.datasource.url=jdbc:postgresql://aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.yprfvumhefrfbmdjtpqc
spring.datasource.password=#ngmg@stockora@26SE99001

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

management.endpoints.web.exposure.include=health,info,metrics

# Inventory Service URL - so Order Service can call it
inventory.service.url=${INVENTORY_SERVICE_URL:http://localhost:8080}

# Order Service runs on a different port than Inventory (8080)
server.port=8081
